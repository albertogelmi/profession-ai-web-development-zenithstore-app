// Extends Jest's expect with @testing-library/jest-dom custom matchers
// (e.g. toBeInTheDocument, toHaveClass, toBeVisible, toHaveTextContent, …)
// This file is automatically run after the test framework is installed in
// the jest-environment-jsdom environment, before each test suite.
import '@testing-library/jest-dom';
